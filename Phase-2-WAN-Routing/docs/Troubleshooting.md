No major issues during this phase of the project. One thing to note is that after implementing static routing, confirming everything worked, and deleting the static routes, I decided to change the IP addressing a bit to what it is now. The major change was changing DSW1 Fa0/24 port to a routed port instead of using VLAN 99 to route between the layer 3 switch and router. This caused me to have to redesign the IP addressing a bit and I also made sure to change the DHCP config a bit to match the new IP addresses so that it would continue to work.